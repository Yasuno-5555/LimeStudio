[package]
name = "starter-synth"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
limestudio_plugin = { path = "../../limestudio_plugin" }
limestudio_core = { path = "../../limestudio_core" }
